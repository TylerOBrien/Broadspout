/**
 * System
*/

import { DurationType, DurationTuple } from '@system/Chrono';

/**
 * Config
*/

export const ImageConfig = {
    default: {
        TTL: [1, DurationType.Hours] as DurationTuple,
    },
};
